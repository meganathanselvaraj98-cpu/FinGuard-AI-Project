# Database Storage

`database/sqlite_schema.sql` is a human-readable reference schema and seed script.

Normal users do not need to run it. SQLAlchemy automatically creates and migrates the local database at:

```text
data/finguard_ai.db
```

Use the website pages **My Stored Data** and **Database Storage** to inspect records safely.
